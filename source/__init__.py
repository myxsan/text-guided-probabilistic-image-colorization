"""Text-conditioned colorization with chroma-aware loss and MDN decoding.

The package contains two scripts:
  * `train.py`              -- runs the 4-experiment ablation, saves checkpoints.
  * `generate_outputs.py`   -- builds generated figures + TEX tables.

See `source/README.md` for usage.
"""
